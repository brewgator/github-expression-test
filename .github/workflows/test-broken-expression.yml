name: test-broken-expression

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes

jobs:
  test-original-failing:
    runs-on: ubuntu-latest
    steps:
      - name: This should fail in schedule context
        run: |
          echo "Original failing expression test"
          echo "PR Number: ${{ github.event.pull_request.number || 'FALLBACK' }}"