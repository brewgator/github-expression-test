name: test-fixed-expression

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes

jobs:
  test-fixed-working:
    runs-on: ubuntu-latest
    steps:
      - name: This should work in all contexts
        run: |
          echo "Fixed expression test"
          echo "PR Number: ${{ github.event.pull_request && github.event.pull_request.number || 'FALLBACK' }}"